<div class="starter-template">
  <h1>'test'のタイトル検索結果</h1>
    <table class="table table-hover">
      <tr>
        <th>書籍名</th>
      </tr>
      <% @books.each do |book| %>
      <tr>
        <td><%= book.title %></td>
      </tr>
      <% end %>
    </table>
</div>

